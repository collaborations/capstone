= link_to(amenity_path(@amenity.id), title: @amenity.desc, category: @amenity.name) do
  - if @no_color.present?
    %div{ class: "row amenity-bg" }
      %div.amenity-img= embedded_svg(@amenity.img, { class: "amenity-img", alt: @amenity.desc})
  - else
    %div{ class: "row amenity-bg amenity-#{@amenity.name.downcase.sub(' ', '')}" }
      %div.amenity-img= embedded_svg(@amenity.img, { class: "amenity-img", alt: @amenity.desc})
  - if @label.present?
    %div.row.amenity-label= @amenity.name
